# Обычно не нужен: ссылка в vless.url (git) → /update сам пишет xray.local.env.
# Ручной fallback, если vless.url нет:
#
# security=reality — sni, fp, pbk, sid, flow в ссылке
# security=none    — только uuid + ip:port

VLESS_URL='vless://UUID@HOST:PORT?type=tcp&encryption=none&security=none#name'
